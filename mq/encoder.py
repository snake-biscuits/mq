import json


# NOTE: need to make sure text data is sanitised!
# -- but would also be nice to have the plain text in `.json` form
# -- will need lots of testing
class Encoder(json.JsonEncoder):
    ...
